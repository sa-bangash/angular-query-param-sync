import { FilterRetainDirective } from './filter-retain.directive';

describe('FilterRetainDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterRetainDirective();
    expect(directive).toBeTruthy();
  });
});
